<p style="color: green"><%= notice %></p>

<%= render @event %>

<div>
  <%= link_to t("Edit this event"), edit_event_path(@event) %> |
  <%= link_to t("Back to events"), events_path %>

  <%= button_to t("Destroy this event"), @event, method: :delete %>
</div>
