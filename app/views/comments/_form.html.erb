<%= form_with url: post_comment_url(@posts), method: :post do |form| %>
     
    <div>
        <%= form.text_area :content,class:"form-control" %>
    </div>

    <div>
        <%= form.submit "Comment"%>
    </div>

<% end %>